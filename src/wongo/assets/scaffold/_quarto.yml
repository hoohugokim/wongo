project:
  type: default
  output-dir: output
engine: knitr
format:
  docx:
    number-sections: false
    # reference-doc and csl are injected at render time by render.py
    # from the journal profile — do not hard-code them here.
bibliography: refs.bib
execute:
  freeze: auto
